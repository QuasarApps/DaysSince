# Declares the app's default (unqualified) locale for AGP's generateLocaleConfig. The strings in
# res/values/ (no locale qualifier) are English (United States); every res/values-<lang>/ is a
# translation of that baseline.
unqualifiedResLocale=en-US
